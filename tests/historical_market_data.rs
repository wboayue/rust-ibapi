use ibapi::fixtures;

#[test]
fn head_timestamp() {
    let result = 2 + 2;
    assert_eq!(result, 4);
}

#[test]
fn histogram_data() {
    let result = 2 + 2;
    assert_eq!(result, 4);
}

#[test]
fn historical_data() {
    let result = 2 + 2;
    assert_eq!(result, 4);
}
